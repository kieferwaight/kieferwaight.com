---
import { getEntry } from 'astro:content';
import ContentPage from '../../components/ContentPage.astro';
const entry = await getEntry('writing', 'index');
---
<ContentPage entry={entry} section="Writing" />
