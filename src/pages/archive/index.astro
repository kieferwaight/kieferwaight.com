---
import { getEntry } from 'astro:content';
import ContentPage from '../../components/ContentPage.astro';
const entry = await getEntry('archive', 'index');
---
<ContentPage entry={entry} section="Archive" />
