---
import ContentPage from "../../components/ContentPage.astro";
import { getPageEntry } from "../../lib/page-loader";

const { entry, section } = await getPageEntry("advisory");
---

<ContentPage entry={entry} section={section} />
