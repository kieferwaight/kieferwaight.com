---
import { getEntry } from 'astro:content';
import ContentPage from '../../components/ContentPage.astro';
const entry = await getEntry('pages', 'resume');
---
<ContentPage entry={entry} section="About" />
